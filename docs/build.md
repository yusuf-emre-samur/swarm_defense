# build

# fetch git repo
```sh
git fetch
```

# pull git changes of dependencies
```sh
vcs pull ./src < dependencies.repos
```

# source workspace
```sh
source install/setup.bash
```

# build 
```sh
source install/setup.bash
```